{% extends "layout.njk" %}
{% import "./../utils.njk" as utils %}
{% from "./../format.njk" import format %}
{# {% from "./component.njk" import shell_header, shell_header_menu %} #}
{% import "./component.njk" as shell %}

<!-- include add'tl css from src/styles/, e.g., ['helpers','components/button'] -->
{% set css_deps = ['icons', 'components/button', 'components/dropdown', 'components/nav', components/identifier', 'components/popover', 'components/menu', 'components/product-switcher', helpers'] %}

{% block content %}

<style media="screen">
    .fd-shell-header{
        margin: 40px 0;
    }
</style>

<div style="background-color: #f3f4f5; margin:-20px; padding:20px;">

    <h1>shell-header</h1>

    <!-- output the component example and the code snippet -->
    {% set example %}
    {%- set _id = utils.id() %}

    {{  shell.shell_header() }}

    {% endset %}
    {{ format(example) }}

{% set properties = {
  "brand": {
    "name": "",
    "logo": ""
  },
  "product_switcher": true,
  "search": "open",
  "copilot": true,
  "action_number": 4,
  "user_menu": true
 } %}

    {% call shell.header(properties) -%}

      {% call shell.header_group(properties, modifier="begin") -%}
//menu
      {%- endcall %}
      {% call shell.header_group(properties, modifier="middle") -%}
bar
      {%- endcall %}
      {% call shell.header_group(properties, modifier="end") -%}

shell.header_actions(properties="{actions: [button, button]")
//icons
//user

      {%- endcall %}


    {%- endcall %}


{% endblock %}

</div>
